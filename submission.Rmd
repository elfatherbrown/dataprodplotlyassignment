---
title: "plotlysubmission"
author: "Alejandro Borges Sanchez"
date: "6/22/2021"
output: ioslides_presentation
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
library(tidyverse)
library(plotly)
library(gapminder)
```

## Plotly
### Classic gapminder

```{r fig.cap="Code in the next page"}
gapminder%>%
    group_by(year,continent)%>%
    summarize(across(c(pop,lifeExp,gdpPercap),~sum(.x,na.rm=TRUE)),.groups = "drop")%>%
    plotly::plot_ly(x=~pop,y=~lifeExp,size=~gdpPercap,color=~continent,frame = ~year,
                    type="scatter",mode="markers")
```

## Code
```{r echo = TRUE, eval=FALSE}
gapminder%>%
    group_by(year,continent)%>%
    summarize(across(c(pop,lifeExp,gdpPercap),
                     ~sum(.x,na.rm=TRUE)),
              .groups = "drop")%>%
    plotly::plot_ly(x=~pop,y=~lifeExp,size=~gdpPercap,color=~continent,frame = ~year,
                    type="scatter",mode="markers")
```

